package io.perfeccionista.framework.value.transformer.number;

import io.perfeccionista.framework.value.transformer.ValueTransformer;

public interface NumberValueTransformer<T extends Number> extends ValueTransformer<T> {
}