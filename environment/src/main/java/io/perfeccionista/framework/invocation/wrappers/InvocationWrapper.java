package io.perfeccionista.framework.invocation.wrappers;

/**
 * Marker Interface
 * В экземплярах этого класса необходимо реализовывать статические (для удобства использования)
 * методы, которые будут получать из Environment соответствующую реализацию ActionRunner
 * и запускать в ней необходимую для оборачивания логику.
 * TODO: JavaDoc
 */
public interface InvocationWrapper {
}
