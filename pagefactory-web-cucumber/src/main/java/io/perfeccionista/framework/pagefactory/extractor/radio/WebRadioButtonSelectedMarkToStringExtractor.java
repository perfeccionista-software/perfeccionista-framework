package io.perfeccionista.framework.pagefactory.extractor.radio;

import org.jetbrains.annotations.NotNull;

import java.util.Map;
import java.util.Map.Entry;
import java.util.stream.Collectors;

public class WebRadioButtonSelectedMarkToStringExtractor
//        implements WebRadioButtonValueExtractor<String>
{

//    @Override
//    public Map<Integer, String> extractValues(@NotNull WebRadioGroupFilter filter) {
//        return new WebRadioButtonSelectedMarkExtractor()
//                .extractValues(filter).entrySet().stream()
//                .collect(Collectors.toMap(Entry::getKey, entry -> entry.getValue() ? "1" : "0"));
//    }

}
