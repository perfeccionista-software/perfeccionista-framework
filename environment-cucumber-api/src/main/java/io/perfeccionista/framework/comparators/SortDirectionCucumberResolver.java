package io.perfeccionista.framework.comparators;

import io.perfeccionista.framework.cucumber.resolver.CucumberResolver;

public interface SortDirectionCucumberResolver extends CucumberResolver<SortDirection> {
}
