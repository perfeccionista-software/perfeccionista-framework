package io.perfeccionista.framework.value.transformer.string.resolver;

import io.perfeccionista.framework.cucumber.resolver.CucumberResolver;
import io.perfeccionista.framework.value.transformer.string.StringValueTransformer;

public interface StringValueCucumberTransformerResolver extends CucumberResolver<StringValueTransformer> {
}
