package io.perfeccionista.framework.value.transformer.number.resolver;

import io.perfeccionista.framework.cucumber.resolver.CucumberResolver;
import io.perfeccionista.framework.value.transformer.number.NumberValueTransformer;

import java.math.BigDecimal;

public interface BigDecimalValueCucumberTransformerResolver extends CucumberResolver<NumberValueTransformer<BigDecimal>> {
}
